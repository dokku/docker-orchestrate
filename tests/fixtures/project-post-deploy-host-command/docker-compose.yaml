---
x-post-deploy-host-command: |
  touch /tmp/orch-project-post-deploy-completed

services:
  web:
    image: nginx:latest
    deploy:
      replicas: 1
      update_config:
        order: start-first
        delay: 0s
        monitor: 5s
