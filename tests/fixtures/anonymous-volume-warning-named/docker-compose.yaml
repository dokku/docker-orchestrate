services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    pull_policy: build
    volumes:
      - test-data:/var/lib/test-data
    deploy:
      replicas: 1
      update_config:
        order: start-first
        delay: 0s
        monitor: 5s
volumes:
  test-data:
