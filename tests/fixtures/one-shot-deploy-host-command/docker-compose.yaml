---
services:
  migrate:
    image: nginx:latest
    command: ["echo", "migration-complete"]
    restart: "no"
    deploy:
      x-pre-deploy-host-command: |
        touch /tmp/orch-one-shot-deploy-pre
      x-post-deploy-host-command: |
        touch /tmp/orch-one-shot-deploy-post
