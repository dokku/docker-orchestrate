---
services:
  web:
    image: nginx:latest
    deploy:
      replicas: 1
      x-post-deploy-host-command: |
        touch /tmp/bats-post-deploy-completed
      update_config:
        order: start-first
        delay: 0s
        monitor: 5s
