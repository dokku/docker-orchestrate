---
x-post-deploy-host-command: |
  touch /tmp/bats-project-deploy-failure-post-deploy

services:
  web:
    image: nonexistent-image:definitely-not-here
    pull_policy: never
    deploy:
      replicas: 1
      update_config:
        order: start-first
        delay: 0s
        monitor: 5s
